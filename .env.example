# 在根目录创建一个新的文件 `.env`，将以下内容修改之后复制到该文件中：
# 
# 默认使用的是Qwen3.5-Flash模型，如果该模型免费额度用完了，可以选择其它模型，
# 不过必须修改相应的python代码。
# 
# 如果使用Qwen3.5-Flash模型：
# 在 https://bailian.console.aliyun.com/cn-beijing/?tab=model#/api-key 
# 创建一个新的API Key，并将其替换到下面的`QWEN_API_KEY`中。
# 
# 如果使用其它模型，要把QWEN_API_KEY和QWEN_API_BASE替换，
# 例如OpenAI的OPENAI_API_KEY和OPENAI_API_BASE。

# TAVILY_API_KEY在 https://app.tavily.com/home 获得，默认1000免费额度。
# 注册后在个人中心的API Keys中创建一个新的API Key，并将其替换到下面的`TAVILY_API_KEY`中。


# 高德地图 Web 服务 API Key，用于天气查询功能。
# 申请地址：https://console.amap.com/dev/key/app （需注册高德开发者账号）
# 创建应用后添加 Web 服务 API Key，替换到下面的 `AMAP_KEY` 中。
AMAP_KEY=********************

QWEN_API_KEY=sk-********************
QWEN_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1
TAVILY_API_KEY=********************
